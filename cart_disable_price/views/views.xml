<odoo>
  <data>
    <!-- explicit list view definition -->
<!--
    <record model="ir.ui.view" id="cart_disable_price.list">
      <field name="name">cart_disable_price list</field>
      <field name="model">cart_disable_price.cart_disable_price</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
-->

    <!-- actions opening views on models -->
<!--
    <record model="ir.actions.act_window" id="cart_disable_price.action_window">
      <field name="name">cart_disable_price window</field>
      <field name="res_model">cart_disable_price.cart_disable_price</field>
      <field name="view_mode">tree,form</field>
    </record>
-->

    <!-- server action to the one above -->
<!--
    <record model="ir.actions.server" id="cart_disable_price.action_server">
      <field name="name">cart_disable_price server</field>
      <field name="model_id" ref="model_cart_disable_price_cart_disable_price"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": model._name,
        }
      </field>
    </record>
-->

    <!-- Top menu item -->
<!--
    <menuitem name="cart_disable_price" id="cart_disable_price.menu_root"/>
-->
    <!-- menu categories -->
<!--
    <menuitem name="Menu 1" id="cart_disable_price.menu_1" parent="cart_disable_price.menu_root"/>
    <menuitem name="Menu 2" id="cart_disable_price.menu_2" parent="cart_disable_price.menu_root"/>
-->
    <!-- actions -->
<!--
    <menuitem name="List" id="cart_disable_price.menu_1_list" parent="cart_disable_price.menu_1"
              action="cart_disable_price.action_window"/>
    <menuitem name="Server to list" id="cart_disable_price" parent="cart_disable_price.menu_2"
              action="cart_disable_price.action_server"/>
-->
  </data>
</odoo>